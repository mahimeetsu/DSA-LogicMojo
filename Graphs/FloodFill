
Example 1:

Input: image = [[1,1,1],[1,1,0],[1,0,1]], sr = 1, sc = 1, color = 2

Output: [[2,2,2],[2,2,0],[2,0,1]]

Explanation:

From the center of the image with position (sr, sc) = (1, 1) (i.e., the red pixel), all pixels connected by a path of the same color as the starting pixel (i.e., the blue pixels) are colored with the new color.

Note the bottom corner is not colored 2, because it is not horizontally or vertically connected to the starting pixel.
===================

class Solution {
    public int[][] floodFill(int[][] image, int sr, int sc, int color) {
        int currColor=image[sr][sc];
        if(currColor!=color)
            dfsChange(image,sr,sc,color,currColor);
        return image;
    }
    public void dfsChange(int[][] image,int sr,int sc,int newColor,int origColor)
    {
        if(sr<0||sr>=image.length||sc<0||sc>=image[sr].length||image[sr][sc]!=origColor)
        return;
        image[sr][sc]=newColor;
        dfsChange(image,sr+1,sc,newColor,origColor);
        dfsChange(image,sr-1,sc,newColor,origColor);
        dfsChange(image,sr,sc+1,newColor,origColor);
        dfsChange(image,sr,sc-1,newColor,origColor);
    }
}
